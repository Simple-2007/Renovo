# Скопировать в .env рядом с каталогом app на сервере и заполнить.
# Реальный файл лежит в /opt/renovo/.env и в репозиторий не попадает.

DJANGO_SECRET_KEY=<длинная случайная строка>
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=renovo.leikom.ru,193.47.60.199,localhost,127.0.0.1

DB_NAME=renovo
DB_USER=renovo
DB_PASSWORD=<пароль пользователя postgres>
DB_HOST=127.0.0.1
DB_PORT=5432
